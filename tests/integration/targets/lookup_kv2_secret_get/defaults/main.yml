---
secret_mount_path: "kv-integration-tests"
test_secret_path: "integration-test-secret-{{ lookup('password', '/dev/null', chars='ascii_letters,digits', length=8) }}"
# Test data that will be written to Vault for testing
test_secret_data:
  - foo: bar
  - hello: world
