---
name: test-dynamic-role
mount_path: database

config:
  db_name: my-postgres
  creation_statements:
    - !unsafe "CREATE ROLE \"{{name}}\" WITH LOGIN PASSWORD '{{password}}' VALID UNTIL '{{expiration}}';"
  default_ttl: "1h"
  max_ttl: "24h"
