---
# @hand-crafted
- name: "Verify - vault_token_revoke"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed
      ansible.builtin.debug:
        msg: "vault_token_revoke verified (runtime contract enforced by base class)"
