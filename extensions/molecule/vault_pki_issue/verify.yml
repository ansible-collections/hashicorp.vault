---
# @hand-crafted
- name: "Verify - vault_pki_issue"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed successfully
      ansible.builtin.debug:
        msg: "vault_pki_issue verified (contract enforced in converge)"
