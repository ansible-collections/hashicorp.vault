---
# @hand-crafted
- name: "Verify - vault_namespace"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed
      ansible.builtin.debug:
        msg: "vault_namespace verified (runtime contract enforced by base class)"
