---
# @hand-crafted
- name: Cleanup - vault_namespace
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Skip namespace tests on OSS Vault
      ansible.builtin.debug:
        msg: "vault_namespace requires Vault Enterprise - skipping on dev server"
