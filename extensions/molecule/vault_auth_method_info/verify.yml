---
# @hand-crafted
- name: "Verify - vault_auth_method_info"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed
      ansible.builtin.debug:
        msg: "vault_auth_method_info verified (runtime contract enforced by base class)"
