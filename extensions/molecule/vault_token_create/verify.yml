---
# @hand-crafted
- name: "Verify - vault_token_create"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed successfully
      ansible.builtin.debug:
        msg: "vault_token_create verified (contract enforced in converge)"
