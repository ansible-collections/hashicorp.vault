---
# Default scenario: manages Vault dev container lifecycle via podman.
# With shared_state, this runs create first and destroy last
# when using molecule test --all.

scenario:
  name: default
  test_sequence:
    - create
    - destroy
