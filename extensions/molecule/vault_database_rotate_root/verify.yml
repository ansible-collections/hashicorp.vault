---
# @hand-crafted
- name: "Verify - vault_database_rotate_root"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Verify converge completed
      ansible.builtin.debug:
        msg: "vault_database_rotate_root verified (runtime contract enforced by base class)"
