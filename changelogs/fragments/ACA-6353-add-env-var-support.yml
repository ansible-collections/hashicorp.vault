---
minor_changes:
  - Add environment variable support for ``VAULT_NAMESPACE``, ``VAULT_AUTH_METHOD``, and ``VAULT_ENGINE_MOUNT_POINT`` to modules and lookup plugins. Module parameters take precedence over environment variables when both are provided (https://github.com/ansible-collections/hashicorp.vault/pull/XXX).
